{
  "private": true,
  "scripts": {
    "build": "esbuild src/infinitude-hvac-card.js --bundle --minify --format=esm --outfile=custom_components/infinitude_direct/www/infinitude-hvac-card.js",
    "release": "node release.js",
    "gh-release": "node -e \"const{version:v}=require('./custom_components/infinitude_direct/manifest.json');const{execSync:r}=require('child_process');r(`gh release create v${v} --title v${v} --generate-notes`,{stdio:'inherit'})\""
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "lit": "^3.0.0"
  }
}
